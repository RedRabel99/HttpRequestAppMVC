@model SelectedHttpRequestListVm

<select id="RequestListId" name="RequestListId" class="form-control">
    @foreach (var list in Model.RequestLists)
    {
        <option value="@list.Id">@list.Name</option>
    }
</select>
